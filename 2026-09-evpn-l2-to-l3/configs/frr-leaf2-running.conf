# Captured with `show running-config`, 2026-09-16, FRR 8.5.3.
# This is what the box reports back, not a typed sequence.
#
# leaf2. Identical to leaf1 apart from the router-id.

leaf2# show running-config
Building configuration...

Current configuration:
!
frr version 8.5.3
frr defaults traditional
hostname leaf2
no ipv6 forwarding
!
vrf tenant-a
 vni 101
exit-vrf
!
router bgp 65000
 bgp router-id 192.168.30.12
 neighbor 192.168.30.2 remote-as 65000
 !
 address-family l2vpn evpn
  neighbor 192.168.30.2 activate
  advertise-all-vni
 exit-address-family
exit
!
router bgp 65000 vrf tenant-a
 !
 address-family ipv4 unicast
  redistribute connected
 exit-address-family
 !
 address-family l2vpn evpn
  advertise ipv4 unicast
 exit-address-family
exit
!
end
